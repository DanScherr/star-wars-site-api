import { DashboardRoutingComponent } from './dashboard-routing.component';

describe('DashboardRoutingComponent', () => {
  it('should create an instance', () => {
    expect(new DashboardRoutingComponent()).toBeTruthy();
  });
});
