import { WelcomeRoutingModule } from './welcome-routing.module';

describe('WelcomeRoutingModule', () => {
  it('should create an instance', () => {
    expect(new WelcomeRoutingModule()).toBeTruthy();
  });
});
